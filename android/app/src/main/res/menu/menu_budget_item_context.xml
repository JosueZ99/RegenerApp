<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- Editar item -->
    <item
        android:id="@+id/action_edit_item"
        android:title="Editar"
        android:icon="@drawable/ic_edit_24"
        app:showAsAction="never" />

    <!-- Eliminar item -->
    <item
        android:id="@+id/action_delete_item"
        android:title="Eliminar"
        android:icon="@drawable/ic_delete_24"
        app:showAsAction="never" />

    <!-- Separador visual -->
    <group android:id="@+id/group_separator" />

    <!-- Copiar a gastos reales -->
    <item
        android:id="@+id/action_copy_to_expenses"
        android:title="Copiar a gastos reales"
        android:icon="@drawable/ic_copy_24"
        app:showAsAction="never" />

</menu>